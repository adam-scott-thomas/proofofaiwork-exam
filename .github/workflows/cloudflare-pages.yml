name: Cloudflare Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      deployments: write
      pull-requests: write
    env:
      # These VITE_* vars are inlined into the build, so they're public.
      # Real secrets (Anthropic keys, Plaid keys, etc.) live in the BACKEND.
      VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
      VITE_WS_BASE_URL: ${{ vars.VITE_WS_BASE_URL }}
      VITE_AUTH_ORIGIN: ${{ vars.VITE_AUTH_ORIGIN }}
      VITE_PROOF_PUBLIC_KEY: ${{ vars.VITE_PROOF_PUBLIC_KEY }}
      VITE_FEATURE_VERIFIED_ENABLED: ${{ vars.VITE_FEATURE_VERIFIED_ENABLED || 'false' }}
      VITE_SENTRY_DSN: ${{ vars.VITE_SENTRY_DSN }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        # vitest covers the canonical + verify regression guard. Cross-language
        # signature drift in src/lib/canonical.ts would silently break every
        # signed proof — fail the build before that gets near production.
        run: npm test

      - name: Build
        # `build:prod` runs scripts/check-prod-env.mjs first and exits
        # non-zero if any required VITE_* var is missing, so a misconfig
        # halts the deploy instead of producing a broken bundle.
        run: npm run build:prod

      - name: Publish to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: proofofaiwork-exam
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          # Branch name is auto-detected; PRs publish a preview, main publishes production.
